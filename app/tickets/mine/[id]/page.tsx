import { TicketDetailPanel } from "@/components/visitor/ticket-detail-panel";

export const metadata = {
  title: "Chi tiết vé | MuseumAR",
  description: "Chi tiết vé tham quan (proposed API contract)",
};

export default function TicketDetailPage() {
  return <TicketDetailPanel />;
}
